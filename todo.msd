- Make sure the appliesTo category on checks still works even if they've been
  naed to 'PostgresCreate' instead of 'create' (I think it should)
- Investigate why invalidOption is potentially now a problem whereas it didn't
  used to be
